% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Forest_seg_v3_3_9.r
\name{Forest_seg}
\alias{Forest_seg}
\title{Forest component segmentation, plot and tree metrics}
\usage{
Forest_seg(
  a,
  filename = "XXX",
  integer_precision = "mm",
  dtm_coarse_res = 0.5,
  tolerance = 0.4,
  dimVox = 2,
  th = 2,
  eps = 2,
  mpts = 9,
  h_trunk = 3,
  N = 3000,
  w_linear = 0.5,
  Vox_print = FALSE,
  Woodpoints_print = TRUE,
  h_rescue_min = 1,
  h_rescue_max = 5,
  dbh_tolerance = 0.05,
  dbh_max_rmse = 5,
  dbh_min_radius = 0.025,
  dbh_max_radius = 0.5,
  canopy_vox_dim = 0.15,
  canopy_min_density = 500,
  dav_understory_max_start = 1.3,
  dav_height_factor = 0.8,
  dav_median_height_factor = 0.5,
  dav_eps = 2,
  dav_minPts = 4,
  calculate_cbh = TRUE,
  cbh_hex_side = 0.15,
  cbh_min_branch_length = 2,
  cbh_save_points = TRUE,
  output_format = "las",
  output_path = tempdir(),
  generate_reports = TRUE,
  ...
)
}
\arguments{
\item{a}{Input point cloud data frame (.xyz format) or file path}

\item{filename}{Output file prefix (default = "XXX")}

\item{integer_precision}{Coordinate decimal precision: "mm" (3 decimals) or "cm" (2 decimals)}

\item{dtm_coarse_res}{Coarse DTM/DSM resolution in m (default = 0.5).
Fine resolution is automatically set to half. DSM uses same resolution for height calculation.}

\item{tolerance}{Vertical tolerance for floor extraction in m (default = 0.4)}

\item{dimVox}{Voxel dimension in cm for LOR wood segmentation (default = 2)}

\item{th}{Minimum points per voxel (default = 2)}

\item{eps}{DBSCAN epsilon radius in voxel units (default = 2)}

\item{mpts}{DBSCAN minimum points (default = 9)}

\item{h_trunk}{Minimum trunk length in m (default = 3)}

\item{N}{Minimum voxels in wood cluster (default = 3000)}

\item{w_linear}{Wood cluster quality filter (default = 0.5)}

\item{Vox_print}{Save voxelization? (default = FALSE)}

\item{Woodpoints_print}{Save wood points? (default = TRUE)}

\item{h_rescue_min}{Minimum normalized height (m above DTM) for RESCUE PASS wood recovery (default = 1)}

\item{h_rescue_max}{Maximum normalized height (m above DTM) for RESCUE PASS wood recovery (default = 5)}

\item{dbh_tolerance}{Vertical tolerance for DBH slice in m (default = 0.05)}

\item{dbh_max_rmse}{Maximum RMSE for DBH fit in cm (default = 5)}

\item{dbh_min_radius}{Minimum valid DBH radius in m (default = 0.025, i.e. 5 cm DBH)}

\item{dbh_max_radius}{Maximum valid DBH radius in m (default = 0.5, i.e. 100 cm DBH)}

\item{canopy_vox_dim}{Voxel size for DAV (Directional Anisotropy of Vegetation) analysis in m (default = 0.15)}

\item{canopy_min_density}{Minimum canopy density in pts/m^3 (default = 500)}

\item{dav_understory_max_start}{Maximum start height for understory in m (default = 1.3)}

\item{dav_height_factor}{Internal DAV parameter. Upper fraction of median tree height for understory ceiling (default = 0.8, stable value).}

\item{dav_median_height_factor}{Internal DAV parameter. Fraction of median tree height for mass distribution threshold (default = 0.50, stable value).}

\item{dav_eps}{DBSCAN epsilon for DAV (default = 2)}

\item{dav_minPts}{DBSCAN minPts for DAV (default = 4)}

\item{calculate_cbh}{Calculate crown base height? (default = TRUE)}

\item{cbh_hex_side}{Hexagon edge length in m (default = 0.15)}

\item{cbh_min_branch_length}{Minimum horizontal extent of foliage in m (default = 2.0)}

\item{cbh_save_points}{Save CBH points? (default = TRUE)}

\item{output_format}{Output format: "las" (single classified LAS file, default) or "xyz" (separate files per class). Any value other than "xyz" falls back to "las".}

\item{output_path}{Output directory (default = tempdir())}

\item{generate_reports}{Generate CSV reports? (default = TRUE)}

\item{...}{Currently unused; reserved for future extensions}
}
\value{
List containing tree metrics, file paths, and analysis results
}
\description{
Segments point cloud into forestry layers using fully optimized float processing.
Integrates the GAB (Geometrical Aggregation of Biomass) Voronoi + Parallel CBH calculation system.

**NEW in v3.3:**

- CBH BFS uses direct vertical adjacency only (no gap jumps); targets the lowest foliated branch
- crown_volume_m3 / understory_volume_m3 written to plot_report
- Crown Packing Index (CPI) and unused helpers removed

**Core (GAB):**

- Integrated GAB (Geometrical Aggregation of Biomass) Voronoi + Parallel CBH calculation
- Hexagonal tessellation replaces polar sector approach
- Crown + Wood voxel integration for spatial continuity
- Multi-core parallel processing for CBH
- BFS connectivity tracing: trunk -> branches -> foliage

**Architecture:**

- All processing uses native float coordinates (x, y, z)
- Global shift applied once at start for numerical precision
- Reverse shift applied once at end for outputs
}
\details{
**Fixed internal parameters (not exposed in interface):**
\itemize{
  \item dtm_fine_res: Automatically set to dtm_coarse_res / 2
  \item dbh_heights: Fixed at c(1.3, 1.8, 2.3) meters
  \item dbh_min_points: Fixed at 8 (works well for both Pratt and Landau fitting)
}
}
